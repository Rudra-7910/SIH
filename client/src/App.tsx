import { InspectionDashboard } from './pages/InspectionDashboard';

/**
 * Legal Metrology Inspection Assistant — App Root
 * 
 * Single-page application. No routing needed for the demo —
 * the inspection dashboard IS the app.
 */
function App() {
  return <InspectionDashboard />;
}

export default App;
